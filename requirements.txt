compas >= 2
compas_model
# compas_viewer fork: group-visibility cascade + signal-safe live scene reload
# (used by examples/watch_viewer.py). Installed from GitHub so it works on any
# machine regardless of local paths.
compas_viewer @ git+https://github.com/petrasvestartas/compas_viewer.git@main
shapely
compas_manifold >= 0.1.0
