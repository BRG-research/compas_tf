compas >= 2
compas_model
shapely
compas_manifold >= 0.1.0
# Brep kernel: compas_tf.brep.get_brep / TFModel.to_step, and the STEP exports
# in the examples. Also ships the Brep scene object compas_viewer needs.
compas_occt >= 0.1.18
