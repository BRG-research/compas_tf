# Optional: the viewer used by the examples. compas_tf itself never imports it
# at module level, so the library works without it - only the drawing does not.
#
# Kept out of requirements.txt because pyproject feeds that file into the
# package metadata, and an optional GUI dependency has no business being a hard
# requirement of a geometry library.
#
# This used to be a git URL pointing at a fork, for a group-visibility cascade
# and a signal-safe live scene reload that a persistent watch_viewer process
# needed. That workflow is gone - the examples use the stock Viewer API and
# compas_tf ships its own scene objects - so the fork is gone with it, and a
# direct URL here would have blocked publishing to PyPI anyway.
#
#   pip install -r requirements-viewer.txt
compas_viewer
