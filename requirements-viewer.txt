# compas_viewer fork: group-visibility cascade + signal-safe live scene reload
# (used by examples/example_0_watch_viewer.py). Installed from GitHub so it
# works on any machine regardless of local paths.
#
# Kept out of requirements.txt on purpose: pyproject.toml feeds that file into
# the package metadata, and PyPI rejects any distribution whose Requires-Dist
# contains a direct URL - which would make `invoke release` fail at upload.
# compas_tf imports compas_viewer lazily (src/compas_tf/viewer.py), so the
# library works without it.
#
#   pip install -r requirements-viewer.txt
compas_viewer @ git+https://github.com/petrasvestartas/compas_viewer.git@main
